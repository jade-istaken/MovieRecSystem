# MovieRecSystem
A basic movie recommendation system trained on the MovieLens 1M dataset

To set up requirements, run `pip install -r requirements.txt`.
To run the full test suite and generate plots, simply run `python movie_rec.py`. Running other files directly may not produce desirable results.
